{
  "name": "basis-round-robin",
  "private": true,
  "version": "1.0.0",
  "description": "Basis BDR to AE round robin tool with a live shared backend and hourly Salesforce sync.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
